import { DashboardTodayClient } from "../_components/DashboardTodayClient";

/** Alias route — same interactive Today dashboard for `/dashboard` demos. */
export default function DashboardPage() {
  return <DashboardTodayClient />;
}
