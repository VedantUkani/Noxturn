/**
 * Placeholder auth destination for settings actions until real flows exist.
 * Matches the public login route (`/`). When already authenticated, middleware
 * may redirect to `/today` — sign-out first if you need the login screen.
 */
export const SETTINGS_LOGIN_HREF = "/";
