# Backend API (FastAPI). Defaults to http://127.0.0.1:8000 if unset.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000

# Optional: Google / Microsoft OAuth for calendar import (legacy flow).
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
